# Dot Files

### Gem defaults
Link `gemrc` to `~/.gemrc`

### Neovim
Install Neovim with homebrew

`brew install neovim`

Link `vim` to `~/.config/nvim`

Open `nvim` and run `:PlugInstall`

### RSpec
Link `rspec` to `~/.rspec`
